# Archived specs — implemented

Every design spec here is **implemented and shipped** (verify via `git log`;
archived 2026-07-21). Files were moved verbatim, so some `Status:` lines still
read "draft" / "approved" / "pending implementation plan" — those predate
implementation and are stale; the archive location is authoritative.

Cross-repo or operational follow-ups owed on archived features (ast-odoo
senders/controllers, staging validation, contract pin bumps) are tracked in
`HANDOFF.md` addenda and `docs/ops-debt-runbook-2026-07.md`, not here.

Open specs stay one level up in `docs/superpowers/specs/`; the matching
implementation plans are archived in `docs/superpowers/plans/archive/`.
