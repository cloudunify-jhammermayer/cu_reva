# Archived plans — implemented

Every implementation plan here is **executed and shipped** (verify via
`git log`; archived 2026-07-21). Unticked checkboxes inside a plan don't mean
open work — completion was recorded in commits, not always in the plan file.

Open plans stay one level up in `docs/superpowers/plans/`; the matching design
specs are archived in `docs/superpowers/specs/archive/`.
