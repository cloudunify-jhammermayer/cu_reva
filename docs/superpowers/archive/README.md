# Archive — implemented specs & plans

Everything in `specs/` and `plans/` here is **implemented and shipped** (verify
via `git log`; archived 2026-07-21). Files are moved here verbatim, so some
`Status:` lines still read "draft" / "approved" / "pending implementation
plan" — those predate implementation and are stale; the archive location is
authoritative.

Cross-repo or operational follow-ups owed on archived features (ast-odoo
senders/controllers, staging validation, contract pin bumps) are tracked in
`HANDOFF.md` addenda and `docs/ops-debt-runbook-2026-07.md`, not here.

Active (not yet implemented) specs and plans stay in
`docs/superpowers/specs/` and `docs/superpowers/plans/`.
