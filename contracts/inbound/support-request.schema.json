{
  "$defs": {
    "Attachment": {
      "description": "A file forwarded by Odoo ({filename, content_base64}). Accepted types are\n.docx / .pdf / .txt / .md \u2014 the filename extension is the authoritative gate\n(see reva.attachment_text). Shared by the ticket-analysis attachment and the\ncreate-issues description_docx; on a create-issues request it is THE basis\nfor the issue split.",
      "properties": {
        "content_base64": {
          "title": "Content Base64",
          "type": "string"
        },
        "filename": {
          "title": "Filename",
          "type": "string"
        }
      },
      "required": [
        "filename",
        "content_base64"
      ],
      "title": "Attachment",
      "type": "object"
    },
    "ChatterEntry": {
      "description": "One Odoo chatter message on a support-request ticket.",
      "properties": {
        "author": {
          "title": "Author",
          "type": "string"
        },
        "author_kind": {
          "enum": [
            "customer",
            "internal",
            "system"
          ],
          "title": "Author Kind",
          "type": "string"
        },
        "body": {
          "title": "Body",
          "type": "string"
        },
        "id": {
          "title": "Id",
          "type": "integer"
        },
        "posted_at": {
          "format": "date-time",
          "title": "Posted At",
          "type": "string"
        },
        "visibility": {
          "enum": [
            "public",
            "internal"
          ],
          "title": "Visibility",
          "type": "string"
        }
      },
      "required": [
        "id",
        "posted_at",
        "author",
        "author_kind",
        "visibility",
        "body"
      ],
      "title": "ChatterEntry",
      "type": "object"
    },
    "ImageAttachment": {
      "description": "A raster image forwarded by Odoo \u2014 the screenshots embedded in a ticket's\ndescription. Accepted types are .png / .jpg / .jpeg / .gif / .webp; the\nfilename extension is the authoritative gate and the bytes are verified\nagainst it (see reva.image_attachment).\n\n`label` is load-bearing, not decoration: it is the text block introducing\nthis image in the Claude call AND the marker Odoo left in `question` in its\nplace, so the two only line up if the sender keeps them in step. Pinned to\n\"Image <n>\" \u2014 it sits outside the SECU-5 nonce fence. `filename` never\nreaches the model.",
      "properties": {
        "content_base64": {
          "title": "Content Base64",
          "type": "string"
        },
        "filename": {
          "title": "Filename",
          "type": "string"
        },
        "label": {
          "title": "Label",
          "type": "string"
        }
      },
      "required": [
        "filename",
        "label",
        "content_base64"
      ],
      "title": "ImageAttachment",
      "type": "object"
    }
  },
  "properties": {
    "attachment": {
      "anyOf": [
        {
          "$ref": "#/$defs/Attachment"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Optional .docx/.pdf/.txt/.md file; its text is extracted and folded into the prompt alongside `question`"
    },
    "chatter": {
      "description": "The ticket's chatter, oldest first. `visibility` is load-bearing: entries marked internal are given to REVA as context but are never quoted or revealed in the drafted answer.",
      "items": {
        "$ref": "#/$defs/ChatterEntry"
      },
      "title": "Chatter",
      "type": "array"
    },
    "field_name": {
      "description": "Field on the Odoo record where the draft answer is written",
      "title": "Field Name",
      "type": "string"
    },
    "github_url": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Repository URL from the record's project (https://github.com/{owner}/{repo}). Odoo requires a linked project, but REVA degrades rather than refusing: without it the answer is grounded in Odoo core knowledge only, with no project-code grounding.",
      "title": "Github Url"
    },
    "images": {
      "description": "Screenshots embedded in the ticket description, in document order. Each `label` (\"Image 1\") must match the [Image N] marker the sender left in `question` where the image was. png/jpeg/gif/webp; max 6 images, 5 MB each, 8 MB total. Defaults to empty, so a sender that does not extract images is unaffected.",
      "items": {
        "$ref": "#/$defs/ImageAttachment"
      },
      "title": "Images",
      "type": "array"
    },
    "model_name": {
      "description": "Odoo model name, e.g. \"helpdesk.ticket\" or \"project.task\"",
      "title": "Model Name",
      "type": "string"
    },
    "persona_context": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Consultant-authored tone/context for this record. Additive on top of the configured persona; never overrides its structured knobs.",
      "title": "Persona Context"
    },
    "question": {
      "description": "The question to answer. On the first turn this is normally the ticket description.",
      "title": "Question",
      "type": "string"
    },
    "subject": {
      "default": "",
      "description": "Ticket subject line",
      "title": "Subject",
      "type": "string"
    },
    "thread_id": {
      "anyOf": [
        {
          "type": "integer"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "REVA thread id, echoed back on follow-up turns. Null on the first turn; REVA resolves or creates the thread from the record.",
      "title": "Thread Id"
    },
    "ticket_id": {
      "title": "Ticket Id",
      "type": "integer"
    }
  },
  "required": [
    "ticket_id",
    "model_name",
    "field_name",
    "question"
  ],
  "title": "SupportRequestBody",
  "type": "object"
}
